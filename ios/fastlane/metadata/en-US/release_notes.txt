• Local device tools — Enable on-device tools per chat for time, location, contacts, messages, calls, email, calendar, reminders, device info, URLs, and alarms
• Safer actions — SMS, calls, email, URLs, and alarms now show clear confirmation cards before opening another app
• Improved MCP setup — Local tools are visually separate from remote MCP servers and show permission status in debug
• Bug fixes — Improved local tool URI encoding, iOS keyboard behavior while streaming, and native permission handling